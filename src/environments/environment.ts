// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
        firebaseConfig: {
        apiKey: "AIzaSyCjw7eJZ7kMsr1Y70amWI9om4tPv9hxa7o",
        authDomain: "logonregistrapp.firebaseapp.com",
        projectId: "logonregistrapp",
        storageBucket: "logonregistrapp.appspot.com",
        messagingSenderId: "1095852614812",
        appId: "1:1095852614812:web:d7cbad1b8be62940ca3959",
        measurementId: "G-020NQX817Y"
      }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
